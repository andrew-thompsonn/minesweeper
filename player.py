#!/usr/bin/env python3

class Player():
    def __init__(self, name):
        # Will be an instance of the database here. This class will communicate to player info
        # table.
        x = 1
        # INSERT INTO player_info (Player ID, Game ID, Player Name, Human) VALUES (DEFAULT, x, Name, x)
